#!/bin/sh

#trap 'trap - TERM; kill 0' INT TERM QUIT EXIT

bspc control --put-status
essid -sf 'G%s' > $PANEL_FIFO &
clock2 -sf 'S\f2%a %h %d \f8|\f2 %I:%M %p\fr' > $PANEL_FIFO &
cat "$PANEL_FIFO" | panel_bar | tbar -b &
